<% if user_signed_in? %>
	<%= simple_form_for([@company, @company.entries.build], html: {class: "form-inline"}) do |f| %>
	  <%= f.error_notification %>
	  
	  <%= f.hidden_field :entry_name, :value => entry_name %>
	  <%= f.input :description, label: false, placeholder: 'Create an entry', input_html: { class: "span4" } %>
	  <%= f.submit 'Submit', class: 'btn btn-small'%>
	<% end %>
<% else %>
	<%= link_to "Create an Account", new_user_registration_path %>
	to contribute
<% end %>